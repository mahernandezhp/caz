trigger Order on Order (before update) {
   // if(trigger.isBefore) {
      //  if(trigger.isInsert) {
        //    if(system.label.CAZ_stopTriggerOrder == 'No') {
                // Se manda el Id de la Planta
          //      list<String> idRegistros = new list<String>();
            //    for(Order forData : trigger.new) {
               //     if(forData.CAZ_Planta__c != null) {
                 //       idRegistros.add(forData.CAZ_Planta__c);
                   // }
                //}
               // if(!idRegistros.isEmpty()) {
                 //   OrderListaDePrecios.buscaLista(trigger.new, idRegistros);
                //}
            //}
        //}
   // }
}